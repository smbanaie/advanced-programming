# تمرین ۱: تسلط بر اصول پایه گیت - مدیریت مخزن محلی

**بخش**: ۱ - مقدمه‌ای بر گیت
**زمان تخمینی**: ۹۰-۱۲۰ دقیقه
**سختی**: مبتدی
**پیش‌نیازها**: ندارد

---

## 📋 نمای کلی تمرین

در این تمرین، مفاهیم پایه سیستم کنترل نسخه گیت را به تسلط خواهید رساند. گیت را نصب خواهید کرد، اولین مخزن محلی خود را ایجاد خواهید کرد، گردش کار پایه گیت را تمرین خواهید کرد و خواهید فهمید که چگونه گیت تغییرات فایل‌های شما را پیگیری می‌کند. این رویکرد عملی، پایه محکمی در کنترل نسخه به شما خواهد داد قبل از اینکه به ویژگی‌های همکاری بروید.

**اهداف یادگیری:**
- نصب و پیکربندی گیت روی سیستم خود
- درک مفاهیم هسته گیت (مخزن، کامیت‌ها، دایرکتوری کاری، ناحیه استیجینگ)
- تمرین گردش کار پایه گیت (init, add, commit, status, log)
- تجربه رویکرد مبتنی بر اسنپ‌شات گیت در کنترل نسخه
- یادگیری پیمایش و درک تاریخچه مخزن

---

## 🎯 الزامات

### قسمت ۱: نصب و پیکربندی گیت (۲۵ امتیاز)

#### وظیفه ۱.۱: نصب گیت
**هدف**: نصب سیستم کنترل نسخه گیت روی کامپیوتر خود

**الزامات:**
- دانلود گیت از https://git-scm.com/downloads
- نصب با تنظیمات پیش‌فرض (برای مبتدیان توصیه می‌شود)
- تأیید نصب با بررسی نسخه

**تأیید نصب:**
```bash
git --version
# باید نمایش دهد: git version 2.x.x
```

**تحویل‌دادنی‌ها:**
- اسکرین‌شات نشان‌دهنده نصب موفق گیت
- خروجی دستور `git --version`
- تأیید اینکه گیت به درستی نصب شده

#### وظیفه ۱.۲: پیکربندی گیت
**هدف**: تنظیم هویت گیت و پیکربندی پایه

**الزامات:**
- پیکربندی نام و آدرس ایمیل خود
- تنظیم ترجیحات پایان خط برای سیستم عامل خود
- تأیید تنظیمات پیکربندی خود

**دستورات پیکربندی گیت:**
```bash
# تنظیم هویت خود
git config --global user.name "نام کامل شما"
git config --global user.email "your.email@example.com"

# تنظیم پایان خط (بر اساس سیستم عامل خود انتخاب کنید)
# برای ویندوز:
git config --global core.autocrlf true

# برای macOS/لینوکس:
git config --global core.autocrlf input

# تأیید پیکربندی
git config --global --list
```

**تحویل‌دادنی‌ها:**
- اسکرین‌شات دستورات پیکربندی گیت و خروجی آنها
- تأیید اینکه user.name و user.email به درستی تنظیم شده‌اند

---

### قسمت ۲: ایجاد اولین مخزن گیت (۳۰ امتیاز)

#### وظیفه ۲.۱: راه‌اندازی مخزن محلی
**هدف**: ایجاد اولین مخزن گیت و درک ساختار آن

**الزامات:**
- ایجاد دایرکتوری جدید برای پروژه تمرین گیت
- راه‌اندازی آن به عنوان مخزن گیت
- بررسی ساختار پوشه `.git`
- درک اینکه هنگام راه‌اندازی مخزن چه اتفاقی می‌افتد

**مراحل ایجاد مخزن:**
```bash
# ایجاد دایرکتوری جدید برای پروژه خود
mkdir git-fundamentals-practice
cd git-fundamentals-practice

# راه‌اندازی مخزن گیت
git init

# بررسی آنچه ایجاد شده
ls -la
# باید پوشه .git را ببینید

# بررسی وضعیت مخزن
git status
```

**تحویل‌دادنی‌ها:**
- اسکرین‌شات نشان‌دهنده ایجاد دایرکتوری و دستور `git init`
- اسکرین‌شات `ls -la` نشان‌دهنده پوشه `.git`
- خروجی `git status` برای مخزن خالی

#### وظیفه ۲.۲: اضافه کردن اولین فایل‌ها
**هدف**: تمرین اضافه کردن فایل‌ها به گیت و درک دایرکتوری کاری

**الزامات:**
- ایجاد چندین فایل متنی با محتوای مختلف
- بررسی وضعیت مخزن پس از هر ایجاد فایل
- درک تفاوت بین فایل‌های tracked و untracked
- اضافه کردن فایل‌ها به پیگیری گیت

**ایجاد و پیگیری فایل‌ها:**
```bash
# ایجاد چند فایل (این دستورات را یکی یکی اجرا کنید)
echo "این اولین مخزن گیت من است!" > README.md
echo "print('سلام دنیا گیت!')" > hello.py
mkdir notes
echo "یادگیری اصول پایه گیت" > notes/learning-notes.txt

# بررسی وضعیت پس از هر ایجاد فایل
git status

# اضافه کردن فایل‌ها به پیگیری گیت
git add README.md
git status

# اضافه کردن همه فایل‌ها
git add .
git status
```

**تحویل‌دادنی‌ها:**
- اسکرین‌شات‌های دستورات ایجاد فایل
- چندین خروجی `git status` نشان‌دهنده پیشرفت از untracked به staged

---

### قسمت ۳: درک کامیت‌ها و تاریخچه (۳۰ امتیاز)

#### وظیفه ۳.۱: ایجاد اولین کامیت‌ها
**هدف**: تجربه رویکرد مبتنی بر اسنپ‌شات گیت در کنترل نسخه

**الزامات:**
- ایجاد اولین کامیت با فایل‌های staged
- درک اینکه کامیت شامل چه چیزهایی است
- تغییر فایل‌ها و ایجاد کامیت‌های اضافی
- تمرین نوشتن پیام‌های کامیت معنادار

**ایجاد کامیت‌ها:**
```bash
# ایجاد اولین کامیت
git commit -m "کمیت اولیه: اضافه کردن README و ساختار پایه پروژه"

# بررسی تاریخچه کامیت
git log
git log --oneline

# تغییر یک فایل و ایجاد کامیت دیگر
echo "محتوای بیشتری به README اضافه شد" >> README.md
git add README.md
git commit -m "به‌روزرسانی README با اطلاعات اضافی"

# بررسی تاریخچه دوباره
git log --oneline
```

**تحویل‌دادنی‌ها:**
- اسکرین‌شات اولین دستور `git commit` و خروجی آن
- اسکرین‌شات `git log` نشان‌دهنده تاریخچه کامیت‌ها
- حداقل ۳ کامیت با پیام‌های معنادار مختلف

#### وظیفه ۳.۲: بررسی مفاهیم گیت به صورت عملی
**هدف**: نشان دادن تسلط بر مفاهیم پایه گیت از طریق مثال‌های عملی

**الزامات:**
- تغییر فایل‌ها به روش‌های مختلف (تغییر محتوا، حذف فایل، تغییر نام)
- استفاده از `git diff` برای دیدن تغییرات قبل از استیجینگ
- تمرین unstaging فایل‌ها
- درک رابطه بین دایرکتوری کاری، ناحیه استیجینگ و مخزن

**بررسی وضعیت‌های گیت:**
```bash
# ایجاد و تغییر فایل‌ها
echo "پیاده‌سازی ویژگی جدید" > feature.txt
git status

# دیدن تغییرات قبل از استیجینگ
git diff

# استیج کردن فایل
git add feature.txt
git status

# دیدن تغییرات staged
git diff --staged

# تغییر فایل staged (ایجاد تغییرات هم staged و هم unstaged)
echo "پیاده‌سازی ویژگی به‌روزرسانی شده" >> feature.txt

# بررسی وضعیت (فایل هم staged و هم modified است)
git status
git diff    # تغییرات unstaged را نشان می‌دهد
git diff --staged  # تغییرات staged را نشان می‌دهد

# کامیت کردن فقط نسخه staged
git commit -m "اضافه کردن پیاده‌سازی اولیه ویژگی"

# استیج کردن تغییرات باقیمانده
git add feature.txt
git commit -m "به‌روزرسانی ویژگی با قابلیت‌های اضافی"
```

**تحویل‌دادنی‌ها:**
- اسکرین‌شات‌های نشان‌دهنده وضعیت‌های مختلف فایل
- مثال‌هایی از `git diff` و `git diff --staged`
- کامیت‌های تاریخچه نشان‌دهنده پیشرفت منطقی پروژه

#### وظیفه ۳.۳: تحلیل تاریخچه مخزن
**هدف**: درک نحوه پیگیری تاریخچه پروژه توسط گیت

**الزامات:**
- ایجاد تاریخچه کامیت جامع
- استفاده از فرمت‌های مختلف `git log` برای مشاهده تاریخچه
- درک metadata کامیت (نویسنده، تاریخ، پیام)
- تحلیل چگونگی تکامل پروژه از طریق کامیت‌ها

**تحلیل تاریخچه:**
```bash
# ایجاد کامیت‌های بیشتر برای ساخت تاریخچه
mkdir src
echo "def main():" > src/main.py
echo "    print('پروژه اصول پایه گیت')" >> src/main.py
git add src/
git commit -m "اضافه کردن دایرکتوری کد منبع و نقطه ورود اصلی"

# ایجاد مستندات
echo "# تمرین اصول پایه گیت" > docs/project-overview.md
echo "این پروژه مفاهیم پایه گیت را نشان می‌دهد." >> docs/project-overview.md
git add docs/
git commit -m "اضافه کردن مستندات پروژه"

# مشاهده تاریخچه در فرمت‌های مختلف
git log
git log --oneline
git log --pretty=format:"%h %ad | %s%d [%an]" --date=short
```

**تحویل‌دادنی‌ها:**
- مخزن با حداقل ۵ کامیت نشان‌دهنده پیشرفت پروژه
- اسکرین‌شات فرمت‌های مختلف `git log`
- تحلیل چگونگی بیان داستان توسعه پروژه توسط تاریخچه مخزن

---

### قسمت ۴: نمایش مفاهیم گیت و تحلیل (۱۵ امتیاز)

#### وظیفه ۴.۱: نمایش درک مفاهیم گیت
**هدف**: نشان دادن تسلط بر مفاهیم گیت از طریق سناریوهای عملی

**الزامات:**
- ایجاد سناریویی که مزایای گیت نسبت به مدیریت دستی فایل‌ها را نشان دهد
- نشان دادن چگونگی پیگیری تغییرات و امکان آزمایش ایمن توسط گیت
- توضیح تفاوت بین گیت و پشتیبان‌گیری ساده فایل‌ها

**نمایش عملی:**
```bash
# ایجاد سناریوی "پشتیبان‌گیری" به صورت دستی (روش قدیمی)
cp -r . ../git-practice-backup-manual
echo "پشتیبان دستی ایجاد شد در $(date)" > ../git-practice-backup-manual/backup-info.txt

# حالا نشان دهید چگونه گیت با همان سناریو برخورد می‌کند
echo "آزمایش ویژگی‌های جدید" > experimental-feature.txt
git add experimental-feature.txt
git commit -m "شروع شاخه ویژگی آزمایشی"

# تغییر ویژگی آزمایشی
echo "پیاده‌سازی ویژگی آزمایشی به‌روزرسانی شده" >> experimental-feature.txt
git add experimental-feature.txt
git commit -m "بهبود ویژگی آزمایشی"

# تصمیم گرفتیم آزمایش کار نمی‌کند - بازگشت آسان با گیت
git log --oneline
git reset --hard HEAD~2  # بازگشت به قبل از آزمایش

# فایل‌های آزمایشی رفته‌اند، اما اگر بخواهیم می‌توانیم آنها را بازیابی کنیم
git reflog
git checkout <commit-hash>  # اگر بخواهیم می‌توانیم بازیابی کنیم
```

**تحویل‌دادنی‌ها:**
- نمایش رویکرد مبتنی بر اسنپ‌شات گیت در برابر پشتیبان‌گیری دستی
- مثال‌هایی نشان‌دهنده امکان آزمایش ایمن توسط گیت
- توضیح مزایای گیت نسبت به مدیریت دستی فایل‌ها

#### وظیفه ۴.۲: تحلیل مخزن و خلاصه
**هدف**: تحلیل مخزن گیت خود و خلاصه یادگیری

**الزامات:**
- ارائه تحلیل کامل ساختار مخزن
- توضیح آنچه هر دستور گیت استفاده شده انجام داد
- تأمل در مورد چگونگی تغییر رویکرد شما به مدیریت فایل‌ها توسط گیت
- مستندسازی چالش‌های مواجه شده و راه‌حل‌های یافت شده

**تحلیل مخزن:**
- ساختار مخزن و سازماندهی فایل‌ها
- پیشرفت و تاریخچه کامیت
- دستورات گیت استفاده شده و اهداف آنها
- مفاهیم پایه نشان داده شده
- بینش‌های شخصی در مورد مزایای کنترل نسخه

**تحویل‌دادنی‌ها:**
- سند تحلیل کامل مخزن
- خلاصه مفاهیم گیت آموخته شده
- تأمل شخصی در مورد مزایای کنترل نسخه

---

## 📋 الزامات تحویل

### فایل‌های مورد نیاز
- **مخزن محلی**: دایرکتوری `git-fundamentals-practice` خود با تاریخچه کامل
- **اسکرین‌شات‌های وضعیت**: اسکرین‌شات‌های `git status`، `git log` و ساختار مخزن
- **خروجی دستورات**: اسکرین‌شات‌ها و خروجی‌های تمام عملیات گیت انجام شده

### تحویل‌دادنی‌های مستنداتی
- **گزارش عملیات گیت**: حساب مرحله به مرحله از دستورات استفاده شده و اهداف آنها
- **تحلیل مفاهیم گیت**: درک شما از مفاهیم گیت (مخزن، کامیت‌ها، استیجینگ و غیره)
- **گزارش چالش‌ها و راه‌حل‌ها**: مشکلات فنی مواجه شده و چگونگی حل آنها
- **مقایسه گیت با پشتیبان‌گیری دستی**: تحلیل مزایای گیت نسبت به مدیریت سنتی فایل‌ها

### فرمت تحویل
- ایجاد پوشه: `homework-01-git-fundamentals`
- شامل فایل‌های مخزن و تمام مستندات
- تحویل پیوند مخزن و گزارش تحلیل عملیات

---

## 🎯 معیارهای ارزیابی

### نصب و پیکربندی گیت (۲۵%)
- نصب موفق گیت و تأیید نسخه
- پیکربندی صحیح هویت گیت (نام و ایمیل)
- تنظیمات درست مخصوص سیستم عامل (پایان خط)
- تأیید و مستندسازی پیکربندی

### ایجاد و مدیریت مخزن (۳۰%)
- راه‌اندازی صحیح مخزن با `git init`
- درک ساختار پوشه `.git`
- فرآیند صحیح استیجینگ و کامیت فایل‌ها
- پیام‌های کامیت معنادار و تاریخچه

### درک مفاهیم گیت (۲۵%)
- نمایش همه وضعیت‌های فایل (untracked، modified، staged، committed)
- استفاده صحیح از `git status`، `git diff` و `git log`
- انتقال‌های صحیح بین وضعیت‌ها
- امکان آزمایش ایمن و بازگشت

### تحلیل و مستندسازی (۲۰%)
- توضیح واضح مفاهیم گیت و دستورات
- تحلیل ساختار مخزن و توجیه
- تأمل شخصی در مورد مزایای کنترل نسخه
- ارائه حرفه‌ای و مستندسازی

---

## ✅ معیارهای موفقیت

تمرین اصول پایه گیت شما زمانی کامل است که:

- ✅ گیت با موفقیت روی سیستم شما نصب و پیکربندی شده
- ✅ مخزن محلی ایجاد شده با `git init`
- ✅ فایل‌ها اضافه، استیج و کامیت شده با پیام‌های معنادار
- ✅ تاریخچه مخزن مفاهیم گردش کار گیت را نشان می‌دهد
- ✅ وضعیت‌های مختلف فایل (untracked، modified، staged، committed) تجربه شده
- ✅ دستورات گیت به درستی استفاده شده: `status`، `add`، `commit`، `log`، `diff`
- ✅ امکان آزمایش ایمن با قابلیت بازگشت نشان داده شده
- ✅ ساختار و تاریخچه مخزن به طور مناسب تحلیل شده

---

**زمان تخمینی تکمیل**: ۹۰-۱۲۰ دقیقه
**سطح سختی**: مبتدی
**امتیاز**: ۱۰۰

تبریک! اصول پایه گیت را به تسلط رساندید. اکنون مفاهیم پایه‌ای را درک می‌کنید که پایه توسعه نرم‌افزار مدرن را تشکیل می‌دهند. این پایه برای همکاری تیمی و ویژگی‌های پیشرفته گیت بسیار ارزشمند خواهد بود.

---

**نکات حرفه‌ای:**
- دستورات گیت را به طور منظم تمرین کنید تا به حافظه عضلانی تبدیل شوند
- همیشه قبل از کامیت `git status` را بررسی کنید
- پیام‌های کامیت واضح و توصیفی بنویسید
- ایمن آزمایش کنید - گیت امکان اشتباه گرفتن را آسان می‌کند
- با پیشرفت خود، ویژگی‌های پیشرفته گیت را بیاموزید