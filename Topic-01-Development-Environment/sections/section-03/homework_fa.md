# تمرین ۳: تسلط بر وضعیت‌های گیت - درک چرخه حیات فایل

**بخش**: ۳ - درک وضعیت‌های گیت
**زمان تخمینی**: ۱۰۰-۱۳۰ دقیقه
**سختی**: متوسط
**پیش‌نیازها**: تمرین ۱ و ۲ (اصول پایه گیت و عملیات مخزن)

---

## 📋 نمای کلی تمرین

در این تمرین، به استاد سیستم مدیریت وضعیت گیت تبدیل خواهید شد. سه ناحیه اصلی گیت را بررسی خواهید کرد (دایرکتوری کاری، ناحیه استیجینگ، مخزن)، تمام انتقال‌های ممکن وضعیت فایل را تمرین خواهید کرد و یاد خواهید گرفت که مشکلات رایج مرتبط با وضعیت را عیب‌یابی کنید. این درک عمیق از کارکرد داخلی گیت شما را به یک کاربر کنترل نسخه مطمئن و مؤثر تبدیل خواهد کرد.

**اهداف یادگیری:**
- تسلط بر سه ناحیه گیت: دایرکتوری کاری، ناحیه استیجینگ، مخزن
- درک و کنترل تمام وضعیت‌های فایل (untracked، modified، staged، committed)
- تمرین انتقال‌های وضعیت با استفاده از دستورات مناسب گیت
- استفاده از دستورات diff برای مقایسه بین وضعیت‌ها
- عیب‌یابی و حل مشکلات رایج مدیریت وضعیت
- توسعه شهود برای رویکرد مبتنی بر اسنپ‌شات گیت

---

## 🎯 الزامات

### قسمت ۱: راه‌اندازی مخزن بررسی وضعیت (۲۰ امتیاز)

#### وظیفه ۱.۱: ایجاد مخزن تمرین
**هدف**: راه‌اندازی مخزن اختصاصی برای بررسی وضعیت‌های گیت

**الزامات:**
- ایجاد مخزن محلی جدید گیت برای تمرین وضعیت‌ها
- راه‌اندازی با ساختار پروژه واضح
- اضافه کردن فایل‌های اولیه برای ایجاد وضعیت پایه

**راه‌اندازی مخزن:**
```bash
# ایجاد دایرکتوری جدید برای تمرین وضعیت‌ها
mkdir git-states-practice
cd git-states-practice

# راه‌اندازی مخزن
git init

# ایجاد ساختار اولیه پروژه
mkdir src docs notes
echo "# پروژه تمرین وضعیت‌های گیت" > README.md
echo "print('بررسی وضعیت‌های گیت')" > src/main.py
echo "# یادداشت‌های پروژه" > notes/learning.md

# بررسی وضعیت اولیه
git status
```

**تحویل‌دادنی‌ها:**
- مخزن محلی راه‌اندازی و پیکربندی شده
- ساختار اولیه پروژه ایجاد شده
- اسکرین‌شات `git status` نشان‌دهنده فایل‌های untracked

#### وظیفه ۱.۲: ایجاد تاریخچه مخزن
**هدف**: ایجاد تاریخچه کامیت برای کار با انتقال‌های وضعیت

**الزامات:**
- اضافه و کامیت کردن فایل‌های اولیه
- ایجاد کامیت‌های متعدد برای ایجاد تاریخچه
- تأیید وضعیت مخزن پس از هر عملیات

**کمیت‌های اولیه:**
```bash
# اضافه و کامیت کردن فایل‌های اولیه
git add README.md
git commit -m "کمیت اولیه: اضافه کردن README پروژه و ساختار اولیه"

git add src/
git commit -m "اضافه کردن دایرکتوری کد منبع و اسکریپت اصلی"

git add notes/
git commit -m "اضافه کردن دایرکتوری یادداشت‌ها برای مستندسازی یادگیری"

# تأیید وضعیت مخزن
git status
git log --oneline
```

**تحویل‌دادنی‌ها:**
- مخزن با ۳+ کامیت ایجاد تاریخچه
- وضعیت پاک مخزن (بدون فایل untracked/modified)
- لاگ تاریخچه کامیت

---

### قسمت ۲: بررسی وضعیت فایل‌ها (۳۵ امتیاز)

#### وظیفه ۲.۱: نمایش تمام وضعیت‌های فایل
**هدف**: تجربه هر وضعیت فایل ممکن در گیت

**الزامات:**
- ایجاد فایل‌ها در هر وضعیت: untracked، modified، staged، committed
- استفاده از `git status` برای مشاهده تغییرات وضعیت
- مستندسازی ویژگی‌های هر وضعیت

**نمایش وضعیت‌ها:**
```bash
# شروع با مخزن پاک (از قسمت ۱)

# ۱. ایجاد فایل untracked
echo "این یک فایل untracked است" > untracked.txt
git status  # باید نشان دهد: Untracked files: untracked.txt

# ۲. تغییر فایل tracked موجود
echo "محتوای تغییر یافته" >> src/main.py
git status  # باید نشان دهد: Changes not staged for commit: modified: src/main.py

# ۳. استیج کردن فایل تغییر یافته
git add src/main.py
git status  # باید نشان دهد: Changes to be committed: modified: src/main.py

# ۴. ایجاد و استیج کردن فایل جدید
echo "فایل staged جدید" > staged-new.txt
git add staged-new.txt
git status  # باید هر دو فایل staged را نشان دهد

# مستندسازی هر مشاهده وضعیت
```

**تحویل‌دادنی‌ها:**
- اسکرین‌شات‌های نشان‌دهنده هر وضعیت فایل
- مستندسازی واضح ویژگی‌های وضعیت
- درک نحوه انتقال فایل‌ها بین وضعیت‌ها

#### وظیفه ۲.۲: تمرین انتقال‌های وضعیت
**هدف**: تسلط بر انتقال فایل‌ها بین تمام وضعیت‌های ممکن

**الزامات:**
- تمرین تمام انتقال‌های وضعیت با استفاده از دستورات مناسب
- استفاده از `git diff` برای مقایسه بین وضعیت‌ها
- درک زمان و دلیل استفاده از هر انتقال

**تمرین انتقال‌ها:**
```bash
# از staged به modified (unstaging)
git reset HEAD src/main.py
git status  # فایل باید modified باشد اما staged نه

# از modified به staged (staging)
git add src/main.py
git status  # فایل باید دوباره staged باشد

# از staged به committed (committing)
git commit -m "به‌روزرسانی اسکریپت اصلی با قابلیت‌های جدید"
git status  # باید پاک باشد

# دور انداختن تغییرات دایرکتوری کاری (modified → committed)
echo "تغییرات موقت" >> src/main.py
git status  # نشان می‌دهد modified

# دور انداختن تغییرات و بازگشت به آخرین کامیت
git checkout -- src/main.py
git status  # باید پاک باشد
```

**تحویل‌دادنی‌ها:**
- اجرای موفق تمام انتقال‌های وضعیت
- اسکرین‌شات‌های `git status` پس از هر انتقال
- درک دستورات مناسب برای هر انتقال

---

### قسمت ۳: مدیریت پیشرفته وضعیت و عیب‌یابی (۳۰ امتیاز)

#### وظیفه ۳.۱: سناریوهای وضعیت پیچیده
**هدف**: رسیدگی به موقعیت‌های پیچیده وضعیت فایل

**الزامات:**
- ایجاد فایل‌هایی که همزمان در چندین وضعیت وجود دارند
- تمرین استیجینگ انتخابی با `git add -p`
- درک کامیت‌های جزئی و استیجینگ

**مدیریت پیشرفته وضعیت:**
```bash
# ایجاد فایلی با تغییرات متعدد
echo "خط ۱" > complex-file.txt
echo "خط ۲" >> complex-file.txt
echo "خط ۳" >> complex-file.txt
echo "خط ۴" >> complex-file.txt

# استیج کردن فایل
git add complex-file.txt
git status  # نشان می‌دهد staged

# تغییر فایل staged (ایجاد تغییرات هم staged و هم unstaged)
echo "خط ۵ - unstaged" >> complex-file.txt
git status  # نشان می‌دهد تغییرات staged و unstaged

# استفاده از diff برای دیدن تفاوت‌ها
git diff    # تغییرات unstaged را نشان می‌دهد
git diff --staged  # تغییرات staged را نشان می‌دهد

# کامیت کردن فقط تغییرات staged
git commit -m "اضافه کردن محتوای اولیه به فایل پیچیده"

# بررسی آنچه باقی مانده
git status
git diff
```

**تحویل‌دادنی‌ها:**
- نمایش فایل‌ها با چندین وضعیت همزمان
- درک تغییرات staged در برابر unstaged
- قابلیت کامیت انتخابی

#### وظیفه ۳.۲: سناریوهای عیب‌یابی وضعیت
**هدف**: یادگیری عیب‌یابی مشکلات رایج مرتبط با وضعیت

**الزامات:**
- ایجاد و حل مشکلات رایج وضعیت
- تمرین تکنیک‌های بازیابی
- درک زمان استفاده از دستورات مختلف

**تمرین‌های عیب‌یابی:**
```bash
# سناریو ۱: استیج کردن اشتباهی فایل
echo "داده‌های حساس" > sensitive.txt
git add sensitive.txt  # اوه، فایل حساس را staged کردم
git status

# unstaging فایل بدون از دست دادن تغییرات
git reset HEAD sensitive.txt
git status  # فایل modified است اما staged نه

# حذف کامل فایل
rm sensitive.txt
git status  # نشان می‌دهد فایل deleted

# سناریو ۲: دور انداختن تغییرات ناخواسته
echo "تغییرات ناخواسته" >> src/main.py
git status  # نشان می‌دهد modified

# دور انداختن تغییرات و بازگشت به آخرین کامیت
git checkout -- src/main.py
git status  # باید پاک باشد

# سناریو ۳: استفاده مؤثر از git diff
echo "تغییر ۱" >> src/main.py
echo "تغییر ۲" >> src/main.py
git diff  # تغییرات دایرکتوری کاری را نشان می‌دهد

git add src/main.py
echo "تغییر ۳" >> src/main.py
git diff     # تغییرات unstaged را نشان می‌دهد
git diff --staged  # تغییرات staged را نشان می‌دهد
```

**تحویل‌دادنی‌ها:**
- حل موفق مشکلات وضعیت
- درک دستورات مناسب برای سناریوهای مختلف
- تکنیک‌های بازیابی برای اشتباهات رایج

---

### قسمت ۴: تحلیل وضعیت و مستندسازی (۱۵ امتیاز)

#### وظیفه ۴.۱: تحلیل وضعیت مخزن
**هدف**: مستندسازی درک جامع سیستم وضعیت گیت

**الزامات:**
- تحلیل وضعیت نهایی مخزن
- مستندسازی تمام انتقال‌های وضعیت انجام شده
- توضیح هدف و تأثیر هر دستور گیت استفاده شده

**تحلیل وضعیت:**
- مستندسازی کامل وضعیت مخزن
- انتقال‌های وضعیت فایل انجام شده
- اهداف و تأثیرات دستورات
- سناریوهای عیب‌یابی مواجه شده و حل شده

**تحویل‌دادنی‌ها:**
- سند تحلیل کامل وضعیت
- تأیید وضعیت مخزن
- درک سیستم مدیریت وضعیت گیت

#### وظیفه ۴.۲: تأمل تسلط بر وضعیت‌های گیت
**هدف**: تأمل در یادگیری مدیریت وضعیت گیت

**الزامات:**
- توضیح چگونگی بهبود درک وضعیت‌ها گردش کار توسعه
- توصیف مزایای سیستم سه‌گانه گیت
- مستندسازی بینش‌های شخصی در مورد مدیریت وضعیت کنترل نسخه

**موضوعات تأمل:**
- چگونگی امکان‌پذیر کردن تمرینات امن توسعه توسط وضعیت‌های گیت
- مزایای ناحیه استیجینگ برای کامیت‌های متمرکز
- اهمیت درک وضعیت‌های فایل برای عیب‌یابی
- کاربردهای واقعی دانش مدیریت وضعیت

**تحویل‌دادنی‌ها:**
- تأمل شخصی در یادگیری وضعیت‌های گیت
- درک مزایای مدیریت وضعیت
- کاربردهای آینده مفاهیم آموخته شده

---

## 📋 الزامات تحویل

### تحویل مخزن
- **مخزن محلی**: دایرکتوری `git-states-practice` خود با تاریخچه کامل
- **لاگ دستورات**: اسکرین‌شات‌های تمام عملیات گیت و مشاهده وضعیت‌ها
- **مستندسازی وضعیت**: رکوردهای详细 از وضعیت‌های فایل و انتقال‌ها

### تحویل‌دادنی‌های مستنداتی
- **گزارش انتقال وضعیت**: لاگ کامل تمام تغییرات وضعیت انجام شده
- **تحلیل دستورات**: توضیح هر دستور گیت استفاده شده و تأثیرات آن
- **گزارش عیب‌یابی**: مسائلی که مواجه شدید و چگونگی حل آنها
- **تأمل مدیریت وضعیت**: بینش‌های شخصی در مورد وضعیت‌های گیت

### فرمت تحویل
- ایجاد پوشه: `homework-03-git-states`
- شامل مخزن با تاریخچه انتقال وضعیت کامل
- مستندسازی تمام دستورات استفاده شده و اهداف آنها
- ارائه توضیحات واضح مفاهیم مدیریت وضعیت آموخته شده

---

## 🎯 معیارهای ارزیابی

### راه‌اندازی و تاریخچه مخزن (۲۰%)
- راه‌اندازی صحیح مخزن و ساختار
- ایجاد تاریخچه کامیت برای تمرین وضعیت‌ها
- وضعیت پایه پاک برای تمرین‌ها

### درک وضعیت فایل (۳۰%)
- نمایش تمام وضعیت‌های فایل (untracked، modified، staged، committed)
- استفاده صحیح از `git status` برای مشاهده وضعیت‌ها
- درک ویژگی‌ها و انتقال‌های وضعیت

### تسلط بر انتقال وضعیت (۳۰%)
- اجرای موفق تمام انتقال‌های وضعیت
- استفاده مناسب از دستورات گیت (add، reset، commit، checkout)
- استفاده مؤثر از دستورات diff برای مقایسه وضعیت‌ها

### عیب‌یابی و تحلیل (۲۰%)
- حل مشکلات رایج مدیریت وضعیت
- مستندسازی درک مدیریت وضعیت
- تأمل در مزایای سیستم وضعیت گیت

---

## ✅ معیارهای موفقیت

تمرین وضعیت‌های گیت شما زمانی کامل است که:

- ✅ مخزن با تاریخچه کامیت پایه مناسب ایجاد شده
- ✅ تمام وضعیت‌های فایل (untracked، modified، staged، committed) تجربه و مستندسازی شده
- ✅ انتقال‌های وضعیت با استفاده از دستورات مناسب گیت انجام شده
- ✅ سناریوهای پیچیده وضعیت به صورت موفق رسیدگی شده
- ✅ مشکلات رایج وضعیت عیب‌یابی و حل شده
- ✅ دستورات diff به طور مؤثر برای مقایسه وضعیت‌ها استفاده شده
- ✅ درک سیستم مدیریت وضعیت گیت نشان داده شده
- ✅ بینش‌های شخصی در مورد وضعیت‌های گیت مستندسازی شده

---

**زمان تخمینی تکمیل**: ۱۰۰-۱۳۰ دقیقه
**سطح سختی**: متوسط
**امتیاز**: ۱۰۰

تبریک! سیستم مدیریت وضعیت گیت را به تسلط رساندید. اکنون بینشی عمیق از نحوه پیگیری و مدیریت تغییرات فایل توسط گیت دارید. این درک شما را به یک کاربر گیت بسیار مؤثرتر تبدیل خواهد کرد.

---

**نکات حرفه‌ای:**
- همیشه قبل از کامیت `git status` را بررسی کنید تا وضعیت مخزن خود را درک کنید
- از `git diff` و `git diff --staged` برای بررسی تغییرات قبل از کامیت استفاده کنید
- ناحیه استیجینگ کنترل کامیت‌های متمرکز را به شما می‌دهد - از آن استفاده کنید
- درک وضعیت‌ها عیب‌یابی را بسیار آسان‌تر می‌کند